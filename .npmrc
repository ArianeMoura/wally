# pnpm dependency policy for the Wally monorepo.
# Internal packages (@wally/*) resolve through the workspace protocol.
link-workspace-packages=true
prefer-workspace-packages=true

# node-linker=hoisted is required for React Native / Metro (Expo SDK 52), which
# does not cope with pnpm's isolated node_modules layout.
node-linker=hoisted
