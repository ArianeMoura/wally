{
  "name": "@wally/core",
  "version": "0.0.0",
  "private": true,
  "description": "Núcleo de domínio puro do Wally: aritmética monetária, divisão por maior resto e cálculo de saldos. Sem I/O, sem framework.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@wally/config": "workspace:*",
    "fast-check": "^4.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
